export enum OrderStatus {
  PENDING = 'pending',
  CONFIRMED = 'confirmed',
  CANCELLED = 'cancelled',
  RETURNED = 'returned',
  PROCESSING = 'processing',
  DISPATCH = 'dispatched',
  SHIPPED = 'shipped',
  DELIVERED = 'delivered',
}
